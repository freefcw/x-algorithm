# 2026-09-03 上游同步记录

## 范围

清点上游 `7ba7768` 与 `85ac72a`，范围为既有同步锚点 `6384ca7..85ac72a`。两个提交共 56 个文件，+3975/-3316。

## 已吸收

- Phoenix search query embedding 改为在批准备阶段展开：`InputBuffer` 只保留一份向量，批缓冲写入前清零。两个上游提交合并为一次本地提交，拆开会暴露复用缓冲的残留数据。本地补一个 `num_real_candidates` 单测，上游没有为这个函数留测试。
- Phoenix 多模态 embedding 拷贝的非法 `emb_dim` 从 panic 改为返回 Error，并拆出 `fetch_mm_embeddings_into`。
- xrex `wait_until_finished()` 不再隐式构造 checkpointer。
- xrex WandbHook 的 metrics 过滤移入线程池。
- 本地修复：`xrex/utils/checkpointing.py` 补齐 `fix_jax` 导入，`checkpointing` 与 `checkpoint_write` 恢复可独立导入；`_should_keep` 的保留判定补单测。

详见 [`../upstream-sync/85ac72a-capability-inventory.md`](../upstream-sync/85ac72a-capability-inventory.md)。

## 未吸收

backbone score 链路、加密 checkpoint 收尾、StableHLO bundle 导出、SID recon 模式、`DpaProductInfo.sid_codes` 均缺少本地可执行合同或消费方，保持 U3。Home Mixer `param.rs` 默认值翻转、Grox 文案与 Kafka producer 改动、visibility-filtering 规则重组、scarecrow XReview 在本地无对应模块。`xrecsys_two_tower.py` 的 `qk_norm` / `attn_logit_cap` 翻转属训练超参取向，留给训练侧决定。

`ActionName` 204–252 号段的永久分歧已在清点文档第 4 节写死：本地保留自有语义，后续同步不再逐次评估。

## 验证

- 根目录 `cargo fmt --all -- --check`：通过
- 根目录 `cargo test --workspace`：255 通过
- `cd phoenix && cargo fmt --all -- --check`：通过
- `cd phoenix && PYO3_PYTHON="$PWD/.venv/bin/python3" cargo test --workspace`：128 通过、3 ignored
- `cd phoenix && .venv/bin/python3 -m pytest tests/ -q`：100 通过
- `git diff --check`：通过

原先记录的「`xrex/utils/checkpointing.py` 与 `xrex/driver/hooks.py` 仅做 `py_compile` 校验」判断有误。真实原因是 `xrex/utils/checkpointing.py` 少了 `from xai_checkpointing import fix_jax`——`fix_jax` 是仓库内既有补丁，把 jax 0.8.1 移除的 `jax.lib.xla_extension.XlaRuntimeError` 映射回 `jax.errors.JaxRuntimeError`，另外两处导入 orbax 的文件都先导入了它。依赖版本本身是上游有意组合，无需改动；训练主路径也未受影响（`trainer.py` 先导入 `xai_checkpointing.load`，补丁已生效），只有单独导入这两个模块会失败。已补齐导入并加回归测试。`hooks.py` 不依赖 orbax，原说明把它一并归因是错的。详见 [`../upstream-sync/85ac72a-capability-inventory.md`](../upstream-sync/85ac72a-capability-inventory.md) 第 5 节。
