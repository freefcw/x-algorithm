# 提交 24c60942 迁移结果（2026-08-28 上游快照）

- **上游范围**：`45b48ba6baa40e212f6dcbaf8fe9fdc8d9da722e..24c60942c5c5fdad3a6addffb4c6e6d2f228f04f`
- **此前锚点**：`45b48ba6baa40e212f6dcbaf8fe9fdc8d9da722e`
- **本地策略**：按业务可执行性和 U0–U3 规则迁移，不整体 cherry-pick
- **能力清点**：[`../upstream-sync/24c60942-capability-inventory.md`](../upstream-sync/24c60942-capability-inventory.md)
- **结果**：Phoenix 协议合同、gRPC trailers 修复和干净容器依赖落地；广告、内部 metadata、copy-port、checkpoint、Grox/VF 等能力按真实重入条件延期或判定不适用

## 1. 已落地线路

### 1.1 Phoenix 协议合同

Rust/Python 两份生产引擎 `recsys.proto` 同步更新：

- `ActionName` 的枚举值 84 命名为 `CLIENT_TWEET_VIDEO_6SEC_VIEW`。
- `UserActionAggregationType` 的枚举值 10 命名为 `DENSE_WITH_PROFILE_LONG_DWELL`。
- `SlateContext` 新增 `reconCountAbove = 14` 和 `reconGapAbove = 15`。

现有编号没有改变，只同步线协议名称和向后追加字段；本轮不启用对应训练或 SlateContext 排序行为。

### 1.2 异步 gRPC 压缩

`GrpcCompressionService` 收集响应体后会保存 trailers，并在压缩或原样返回时重新附加。测试覆盖：

- 正常压缩响应保留 `grpc-status`。
- 小响应不压缩时仍保留 `grpc-status`。
- 非 OK 响应保留 `grpc-status` 和 `grpc-message`。

该开关仍默认关闭，本轮只修复已存在可选入口的响应正确性。

### 1.3 Phoenix 安装文档

干净 CUDA 容器的 apt 依赖加入 `ca-certificates` 和 `curl`，保证后续 protoc 下载命令具备必要工具和证书。

### 1.4 已有等价行为

当前分支提交 `f72e345` 已包含 CJK 静音关键词整词匹配及“京都”不误匹配“東京都”的回归测试，因此本轮不重复修改。

## 2. 延期与不适用

- `MultiRiskAdsBlender` 和 ads VF：Ads Source 仍关闭，缺少真实广告和风险数据合同。
- StratoContext polling：没有公开调用方 metadata 合同，继续使用请求体字段。
- Home Mixer request context metrics：没有本地指标消费者，后续通过 `FeedStatsSink` 接入。
- copy-port shuffle、stream restore：没有多实例热点、加载 OOM 或峰值内存证据。
- KMS checkpoint：没有本地密钥服务、认证和审计合同。
- Grox reply-spam、visibility-filtering、abuse-enforcement、SimClusters：本地无完整业务入口或不属于推荐域。
- Brazil 2026 election filter：维持既有产品决定，不迁移。

完整重入条件见能力清点文档。

## 3. 验证

- Phoenix proto：6 通过；Rust/Python 两份定义字节一致。
- 异步 gRPC 压缩定向测试：5 通过。
- 根 Rust workspace：255 通过。
- Phoenix Rust workspace：122 通过、3 ignored。
- Phoenix Python：92 通过。
- 根目录和 Phoenix `cargo fmt --all -- --check`、`git diff --check` 通过。
- Proto 严格 Clippy 通过；Engine 定向 Clippy 通过，严格 `-D warnings` 仍被两个未改动文件中的既有告警拦住。

## 4. 锚点

`upstream-first-maintenance.md` 的已清点/吸收锚点已前移至 `24c60942c5c5fdad3a6addffb4c6e6d2f228f04f`。所谓“吸收”是指范围内每项能力都有明确处置，不表示延期能力已经上线。
