# 提交 47c1bcd 改动分析报告（2026-08-13 上游快照）

- **提交哈希**：`47c1bcdadfe4911568fd6db4f8838b194325beab`
- **作者**：CI agent <support@x.ai>
- **日期**：2026-08-13
- **提交信息**：`Open-source X Recommendation Algorithm`
- **改动规模**（相对上一快照 `0bfc279`）：2053 个文件，+363,246 / -11,640 行
- **本地基线**：`feature/migrate-20260515` 已完整吸收 `e414c17`/`0bfc279`（见 `docs/upstream-sync/`）

> 注：上游每次更新都是同名 squash 提交整体替换历史。本仓库沿用
> `docs/upstream-sync/upstream-first-maintenance.md` 的 U0-U3 语义迁移流程，不做整体 rebase。
>
> 上游随后还有一个同日提交 `a389166`（2026-08-13 17:23），只删除 `.gitattributes` 中
> `*.npz`/`*.zip` 两条 LFS 规则，与产物下架一致，无源码差异，因此不单独立项。本地
> `.gitattributes` 保留 `*.zip` 规则（U2），因为 legacy Phoenix 产物指针仍依赖它。

---

## TL;DR

上游官方声明本次更新做了三件事：

1. **公开关键配置参数**：包括把各行为预测值混合成帖子分数的真实权重（`home-mixer/params/`、`ranking_scorer.rs` 扩至 1679 行）。
2. **公开决定帖子能否展示的过滤系统**：`visibility-filtering/` 及其标签生产链（botmaker、scarecrow、agatha、bdsm、user-cred-v2、media-model-proxy、clip、abuse-enforcement-service）。
3. **用真实训练代码替换 Phoenix 演示模型**：`phoenix/` 整体重构为训练+推理框架（xrex），附合成数据生成，可跑 POC 训练；旧 `run_pipeline.py`/`oss-phoenix-artifacts.zip` 演示链路被移除。

对本仓库最重要的 5 个影响：

1. **⭐ 大量此前 U3（暂缓，无公开合同）的能力现在有了上游真实实现**：`home-mixer/clients/`（19 个客户端全部开源）、`vm-ranker/`（完整服务）、`visibility-filtering/`、Thunder `schema/`（解锁本地 legacy Kafka listener 的编译阻塞）、SimClusters。此前按"接口先行"预留的端口（`VMRankerClient` 等 7 个）正好是落点。
2. **⭐ 真实打分权重与参数默认值公开**：`params/param.rs` 注明 "mirrored from config feature-switch defaults"。本地此前自拟的参数可以逐项换成上游真值（例：Thunder max_results 本地 500 → 上游 1200；`TOP_K_CANDIDATES_TO_SELECT=50`、`RESULT_SIZE=35`）。
3. **⭐ Phoenix 是破坏性替换**：本地 published-artifact 推理链路（离线 pipeline + gRPC 网关 + 88 项测试）在新上游没有对应物。新框架要求 Linux + NVIDIA GPU + CUDA 12 才能训练/服务；macOS 上只能引入代码、跑合成数据生成与部分 CPU 路径。
4. **candidate-pipeline 执行语义演进**：新增 `pipeline_summary.rs`（请求级 stage 汇总日志）、`CachedHydrator::{cache_key_for, already_hydrated}`、`Selector::stat`、`SPAN_LEVEL`/quiet-spans。
5. **上游自行清理了与本地判断一致的死代码**：`cache_request_info_side_effect.rs`、`candidate_pipeline/{candidate,candidate_features,query,query_features}.rs` 已删除；也删除了本地已判定为上游死文件的 `has_media_hydrator`（并入 `media_info_hydrator`）等。

---

## 一、模块级变化总览

| 模块 | 文件数 | 行数变化 | 性质 |
|---|---:|---|---|
| `botmaker/` + `botmaker-rules/` | 565 | +63,105 | 全新：反机器人规则引擎与聚合器 |
| `phoenix/` | 314 | +117,520 / -4,361 | **整体替换**：训练框架 xrex + Rust serving crates + 合成数据 reference |
| `simclusters/` | 232 | +35,797 | 全新：社区聚类召回（含 simclustersann 近邻服务） |
| `home-mixer/` | 214 | +28,683 / -2,249 | 大幅增量：clients/util/params 开源、组件增删、URT 序列化 |
| `grox/` | 198 | +12,788 / -4,313 | **结构重构**：tasks/plans 删除，改为 config/core/flows/libs |
| `media-model-proxy/` | 98 | +4,167 | 全新：图像/视频模型代理 |
| `agatha/` | 90 | +5,629 | 全新：账号多维评分模型（scalding + hub） |
| `visibility-filtering/`(+client) | 70 | +18,860 | 全新：可见性过滤规则引擎 |
| `bdsm/` | 45 | +9,897 | 全新：账号评分运行时 |
| `phoenix-rankall/`(+strato) | 52 | +9,351 | 全新：全量排序离线组件 |
| `under-the-hood/` | 26 | +4,138 | 全新：标签透明度工具（scalding + strato） |
| `scarecrow/` | 32 | +3,231 | 全新：规则引擎 legacy 部分 |
| `abuse-enforcement-service/` | 31 | +12,622 | 全新：违规执法服务 |
| `thunder/` | 21 | +29,940 / -293 | **schema 全部开源**（tweet/user/media 等）+ strato_client + o2 |
| `clip/` | 20 | +2,856 | 全新：CLIP 媒体嵌入 |
| `vm-ranker/` | 10 | +1,302 | 全新：Value Model Ranker 服务（DPP 重排） |
| `candidate-pipeline/` | 10 | +395 / -192 | 增量：pipeline_summary 等执行语义 |
| 其他（user-cred-v2、adult-content、pnsfwmedia、safety-label-user-agg、docs） | 22 | +2,611 | 全新：辅助评分与文档 |

---

## 二、与本地迁移直接相关的变化

### 2.1 home-mixer（P1 主战场）

**新增（此前不存在或未公开）**：

- `clients/`（19 个）：ad_index、engagement_counts、engagement_signals、gizmoduck、impressed_posts、night_owl、past_request_timestamps、prompts、resurrection_date、rotating_channel、s2s、served_history、simclusters_ann、tweet_entity_service、uas_fetcher、user_action_aggregation、vm_ranker、who_to_follow + mod。**注意**：本地 `clients/` 目录是自建 U1 适配器，文件名部分相同（gizmoduck_client、tweet_entity_service_client、uas_fetcher），对齐时需逐个对比契约。
- `params/`：`param.rs`（feature-switch 默认值镜像）、`config.rs`（真实常量）。对应本地单文件 `params.rs`。
- `util/`（31 个）：author_rules、candidates_util、conversation_grouping、country_codes、egress、feed_log、phoenix_request、rescore、shadow、tweet_type_metrics、url、xds + `urt/`（17 个 URT 序列化 marshaller——此前 HM-E6 因"无公开 URT 合同"而 deferred，现在合同来了）。
- `frames/`（5 个）：jetfuel frame 投放位（catalog/placement/plan/served）。
- 新 pipeline：`following_candidate_pipeline.rs`、`ranked_following_candidate_pipeline.rs`、`reverse_chron_posts_pipeline.rs`、`phoenix_scores_pipeline.rs`；新 server：`following_feed_server.rs`、`ranked_following_feed_server.rs`、`phoenix_scores_server.rs`。**Home Mixer 从"单 ForYou 产品"扩展为多 Feed 产品家族（ForYou / Following / RankedFollowing / ReverseChron）**。
- 新组件：sources（feed_survey、following_night_owl、jetfuel_frame、popular_topics、reverse_chron_posts、seed_candidates、simclusters）、filters（11 个新增）、candidate_hydrators（bidirectional_follow、broadcast_liveness、conversation_gap_ancestor、media_info、semantic_id、topic_feedback_context）、query_hydrators（explicit/implicit_engagement_signals、user_installed_apps）、side_effects（author_served_metrics、debug、served_ad_history_cache）、selectors（following_blender、passthrough）、ads（following_ad_blender、time_gap_blender + tests/）。
- `config.rs`、`dark_traffic_setup.rs`、`bin/pipeline_components_json.rs`。

**删除（上游自行清理）**：

- 死代码：`cache_request_info_side_effect.rs`、`candidate_pipeline/{candidate,candidate_features,query,query_features}.rs`（本地早已判定；本地残留的 `cache_request_info_side_effect.rs` 本次一并删除）。
- 组件重组：`has_media_hydrator` + `video_duration_candidate_hydrator` → `media_info_hydrator`；`ads_brand_safety_hydrator` 删除（保留 vf 版）；`new_user_topic_ids_filter`、`inferred_grok_topics_query_hydrator`、`user_action_seq_query_hydrator`、`user_features_query_hydrator` 删除。
- scorers 重组：`weighted_scorer`/`author_diversity_scorer`/`oon_scorer` 删除，改为 `ranking_scorer`（1679 行，真实权重混合 + 多样性 + OON + 冷启动）、`phoenix_scores_ranking_scorer`（38 行）、`value_model_gate`（537 行）、`author_cold_start`（494 行）。**与本地 RankingScorer facade 策略同向**，且真实权重可以替换本地自拟值。
- 改名：`for_you_response_stats_side_effect.rs` → `response_stats_side_effect.rs`（R053）。

### 2.2 candidate-pipeline（P1 首个落点）

- 新增 `pipeline_summary.rs`（229 行）：`StageStats` + tokio task_local 聚合，每请求输出一行 stage 汇总。
- `execute` 拆分为 `execute`（包 metric 标签作用域）+ `execute_stages`。
- `CachedHydrator` 新增 `cache_key_for(query, candidate)`（缓存键可含请求维度）与 `already_hydrated(candidate)`（跳过已补全候选）。
- `Selector` 新增 `stat()`（result_size / result_empty 统计）。
- `SPAN_LEVEL` 常量 + `quiet-spans` feature 控制 tracing 级别（本地无 tracing，按 U1 用 log 对应）。

### 2.3 thunder（P3 解锁项）

- `schema/` 全部开源：tweet(5471 行)、user(14750 行)、tweet_events、tweet_media、media_common、media_entity、events。本地 legacy `tweet_events_listener.rs` 缺的 `crate::schema::{tweet, tweet_events, tweet_media}` 正好补齐。
- 新增 `args.rs`、`config.rs`、`metrics.rs`、`o2/`（对象存储恢复）、`strato_client.rs`；listener/deserializer/post_store/service 均有修改。

### 2.4 phoenix（P2 主战场，破坏性）

新结构：

- `xrex/`（192 文件）：模型（models/recsys_model.py 等）、训练（train/）、推理（inference/launch_inference.py、oss_bench/bench.py）、数据（data/）、优化器、pallas/cuda kernel、configs。
- `python/common/`：xai-proto、xai-configlib、xai-gimmick；`python/training/`：xai-checkpointing（均为 path 依赖的本地包）。
- `crates/`（Rust workspace）：xai-recsys（common）、xai-recsys-engine（PyO3 引擎，训练/服务必需）、xai-recsys-server、xai-recsys-mm-server、xai-recsys-proto、xai-recsys-sid-proto。
- `reference/`：合成数据（world_snapshots.py、dump_gen.py）、训练启动器（train_synth.py、train_step.py）、SID 工具（sid_*.py）、端到端验证（retrieve_then_rank.py）、artifact 生成（gen_recs_artifacts_gen.py）。
- 文档：QUICKSTART.md（合成数据→训练→续训→服务→retrieve→rank 全流程）、TRAINING.md。

**环境要求**：训练与服务需 Linux + NVIDIA GPU + CUDA 12（`uv sync --extra engine`）；jax 0.8.1（与本地一致）、dm-haiku 0.0.16、orbax-checkpoint。macOS 上 engine extra 不可用。

**被替换/删除**：根目录 recsys_model.py、recsys_retrieval_model.py、grok.py、runners.py、run_pipeline.py、run_ranker.py、run_retrieval.py、旧测试、`artifacts/oss-phoenix-artifacts.zip`（LFS 对象不再存在于新快照）。

### 2.5 grox（P6 路线重写）

上游删除 tasks/plans/classifiers/generators/schedules/summarizer/embedder 等 2026-05 结构，重构为 `config/`、`core/`、`flows/`、`libs/`。本地 P6-A 中立骨架不受影响（本来就是隔离实现），但 `docs/upstream-sync/p6-grox-recovery-audit.md` 的恢复路线需按新结构重写。

### 2.6 全新独立系统（P3+ 按需接入）

| 系统 | 作用 | 主干依赖性 |
|---|---|---|
| `vm-ranker/` | Value Model 重排 + DPP 多样性 gRPC 服务 | 旁路（本地 `VMRankerClient` 端口已备）；依赖私有 `xai_http_server`/`xai_profiling`，需 U1 壳替换 |
| `visibility-filtering/`(+client) | 可见性规则引擎（展示/丢弃/插页） | 主干安全语义的真实实现；本地 VF 桩的替换候选 |
| `simclusters/` | 社区聚类网外召回 + ANN 服务 | 旁路召回源（home-mixer 有 simclusters_source + client） |
| `botmaker/` 系、`agatha/`、`bdsm/`、`user-cred-v2/` | 标签/账号评分生产链 | 离线/独立服务，主干只消费标签 |
| `media-model-proxy/`、`clip/`、`pnsfwmedia/`、`adult-content/` | 媒体理解模型 | 独立服务 |
| `abuse-enforcement-service/` | 执法 | 独立服务 |
| `under-the-hood/` | 标签透明度工具 | 独立离线工具 |
| `phoenix-rankall/`(+strato) | 全量排序离线组件 | 独立离线 |

---

## 三、接入分期（已确认执行）

| 期 | 内容 | 原则 |
|---|---|---|
| P0 | 本分析文档 + 锚点/台账更新 + 死代码清理（`cache_request_info_side_effect.rs` 等） | 文档先行 |
| P1 | candidate-pipeline 执行语义 → home-mixer 参数真值对齐 → scorers 重组 → 修改组件增量 → clients 契约校对 | U0 保名保序，U1 换实现，U2 加测试，主链 demo 不回归 |
| P2 | 引入 phoenix 新框架（上游路径原样落地）+ 保留本地推理链路（改挂 legacy 路径或并存）+ macOS 可行冒烟（合成数据生成；训练/服务标注需 Linux+GPU） | 不删可运行链路；环境不足不伪造验收 |

**P2 执行结果（2026-08-14）**：上游 `xrex/`、`python/`、`crates/`、`reference/`、QUICKSTART/TRAINING/NOTICE 已原样落地（303 文件）；`pyproject.toml` 以上游为基础合并本地 dev/service 组；本地旧 README 挪至 `phoenix/docs/legacy-pipeline.md`；根 Cargo workspace `exclude = ["phoenix"]`。macOS 验收：`uv sync` 通过（上游 4 个 path 包安装成功）、旧推理链路 88 项 pytest 全部通过（依赖升级 dm-haiku 0.0.16 / protobuf 6.33 后 `services/recsys_proto.py` 增加 gencode/runtime 失配重建）、`reference/world_snapshots.py` 与 `reference/dump_gen.py --self-check` 通过（2048 行合成 dump）、`xrex.models` CPU 可导入。**训练与服务（`reference/train_synth.py`、`xrex/inference/launch_inference.py`）需要 Linux + NVIDIA GPU + CUDA 12**：`xai-recsys-engine` PyO3 crate 无条件依赖 `ibverbs`（Linux RDMA），macOS 无法构建，`uv sync --extra engine` 不可用；POC 训练按 `phoenix/QUICKSTART.md` 在 Linux GPU 机器执行。
| P3 | thunder schema 解锁 legacy listener → vm-ranker 以 U1 壳引入并对接 `VMRankerClient` 端口（默认关） → visibility-filtering 可构建引入（默认不装配） | 旁路开关化，默认关闭 |

**P3.1 执行结果（2026-08-14）**：上游 `thunder/schema/`（tweet/user/media/events 等 7 文件、28,612 行）
原样落地，挂在 `legacy` feature 后；`cargo check -p thunder --all-targets --all-features` 由此转为通过，
`crate::schema` 编译阻塞解除。listener 仍不可运行：`xai_kafka`、`xai_thunder_proto` 未开源，
由 `cfg(xai_internal_deps)` 继续排除，摄入维持本地 rdkafka 路径。

**P3.2 执行结果（2026-08-14）**：上游 `vm-ranker/` 10 个文件全部落地，其中 6 个与上游逐字节相同；
U1 壳限于两处私有依赖——`main.rs` 的 `xai_http_server` 换成 tonic + axum，`embedding_store.rs` 的
O2 多模态客户端换成查询 trait + 内存表（空表即"全部缺失"，DPP 沿用上游随机向量降级）；其余 3 个文件
只差 proto crate 路径。wire 定义上游未发布，按使用面重建 `proto/definitions/vm_ranker.proto` 供两端共用。
home-mixer 侧新增 `GrpcVMRankerClient` 落在既有 RANK-03 端口上，`HOME_MIXER_ENABLE_VM_RANKER=1`
且 `VM_RANKER_GRPC_ADDR` 齐备时装配为第三个 Scorer，缺地址则告警跳过。验收见台账 `EV-VMR`。

**不接入清单（本轮）**：botmaker/agatha/bdsm/media-model-proxy/clip/abuse-enforcement-service/under-the-hood/phoenix-rankall/simclusters 离线生产链——均为独立系统，主干只消费其产物；待产品场景与数据合同明确后按 U3 流程逐项评估。

---

## 四、风险与已知问题

1. **Phoenix 旧 LFS artifact 在新快照中不存在**：本地推理链路依赖的 `oss-phoenix-artifacts.zip` OID 只存在于 `0bfc279`。工作树保留 LFS 指针即可，历史验收记录不变；新框架产物需自行训练生成。
2. **上游 params 值 ≠ 本地 demo 参数**：对齐真值会改变 demo 输出（如 Thunder max_results 500→1200、result_size 50→35 语义拆分为 TOP_K=50/RESULT_SIZE=35）。需在对齐时同步更新 demo 断言与文档，并记录行为变化。
3. **vm-ranker/VF 依赖私有基础库**（xai_http_server、xai_profiling、xai_stats 等）：引入时按既有 U1 规则替换壳层，不把私有类型泄入业务代码。
4. **grox 新结构与本地 P6-A 骨架差异大**：本轮不重写 grox，仅更新路线文档，避免一次性铺开。
5. **多 Feed 产品家族（Following/RankedFollowing/ReverseChron）**：本轮聚焦 ForYou 主链对齐；新 Feed 家族作为后续纵向能力，先在台账登记。
