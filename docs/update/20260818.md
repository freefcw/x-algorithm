# 提交 11a71f8 迁移结果（2026-08-18 上游快照）

- **提交哈希**：`11a71f87d6a7fc4c1e8159dad8f3c5ff90a0f7ed`
- **上游父提交**：`b089ce64891f9c50fab73aa00dbe65acb82f198f`
- **本地策略**：按 U0-U3 语义迁移，不整体 cherry-pick
- **能力清点**：[`../upstream-sync/11a71f8-capability-inventory.md`](../upstream-sync/11a71f8-capability-inventory.md)
- **结果**：无可落地项；全部为既有 U3 线路的语义迭代，记录后锚点前移

## 1. 不落地项分布

- **V1–V5（visibility-filtering 线路）**：Following feed 的 post-selection VF hydrator 及配套。由既定不采用决策覆盖（`e414c17` 台账 P1.4）：阻塞点是标签数据源合同，不是规则代码。
- **I1（品牌安全 verdict V2 语义修正）**：v2 在无 V2-written 标签时整体回落 v1；标签集调整；删除 `strip_v1_grok_written`。重入时以此最终语义为准。
- **I2（cold-start tracked-ids 计数修正）**：只计 tweet_id。依附已暂缓的 `ColdStartTrackedIds` observability 线路。
- **I3（vm_ranker SID slate 字段映射）**：依附 b089ce6 A5 线路。
- **G1–G2（grox）**：reply_spam 生成器收敛终态 + 阈值上调、ptos metric 口径细化。走 P6-B 重入条件；G1 取代 b089ce6 C4 的中间形态。

## 2. 验证

无代码改动，无需重跑测试。锚点前移至 `11a71f8`；下一清点对象 `aad7179`（2026-08-19）。
