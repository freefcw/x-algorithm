# 2026-09-07 上游同步记录

## 范围

清点上游 `902a06f`，范围为既有同步锚点 `9b0dc31..902a06f`。单个常规快照，25 个文件、+480/-179。按目录：visibility-filtering 12、phoenix 8、home-mixer 5。

本轮是低产出的一轮：两项吸收都不改变运行时行为，价值只在于把文件保持与上游逐字节一致，让下一轮 diff 仍是小 delta。只按业务收益取舍的话，本轮可以什么都不做。

## 已吸收

- `xrex/data/retrieval_dataset.py`：SID 快照路径抽出 `_sid_window()`，去掉 `_idx()` 里 v5→v8 的字符串替换 hack。路径结果完全等价，逐条核过全部 9 条构造路径。两处容易看错的地方：旧替换对 `..._snapshots_backup` 生效（前缀匹配，备份路径原本就是 v8），对 `post_sid_v5_256x6_tail_snapshots` 不生效（中间隔着 `_tail_`，`TAIL` 一直是 v5，改后仍是 v5）。
- `xrex/utils/log_util.py`：formatter 把 `LogRecord` 上的非标准属性渲染成 `k=v` 追加到首行末尾，`event` 排最前，多行 traceback 不受影响。本地和上游整个 phoenix 树目前都没有 `extra={...}` 调用方，属先行于消费方的基础设施；无额外属性时输出与改前逐字节相同。

顺带修一处上一轮遗留：`3461491` 从 `xai-recsys-engine/Cargo.toml` 删掉了 `xai-recsys-sid-proto`，但 `phoenix/Cargo.lock` 没跟着重新生成，本轮跑 cargo 时被自动修正，净改动一行。workspace 层面的成员声明与依赖定义保持不动，`reference/` 下两个 SID 服务仍在用。

详见 [`../upstream-sync/902a06f-capability-inventory.md`](../upstream-sync/902a06f-capability-inventory.md)。

## 未吸收

`author_cold_start.rs` 是本轮最大改动（275 行），改的是 MoE 共分流实验：抽出 `ColdStartParams` 一次性读参、treatment 臂的语料判定加 `is_phoenix_moe` 约束、把 `author_corpus` 计算提到 `enabled` 早退之前使两臂 bucket impression 对称。本地 `AuthorColdStart` 是精简移植，读静态 `ColdStartConfig` 而非 feature switch，没有 `ViewerArm`/`AuthorCorpus`/`ExperimentBucketImpressor` 概念，三项改动都没有落点。

`engagement_counts_hydrator.rs`、`mutual_follow_stats_side_effect.rs` 的宿主组件本地未迁移；`params/param.rs` 只改了同步时间戳注释；`models/candidate.rs` 的 `exact_k`/`exact_gap` 因本地没有 `SlateContext` 模型保持 U3。

Phoenix 侧：StableHLO bundle 导出（`recsys_bundle_export.py` 及 `trainer_recsys.py` 的 MANIFEST 重打时间戳）本地无该模块，维持 20260903 记的 U3；`xai-checkpointing` 的 KMS unwrap 退避重试与 `_DEK` 提交前断言依赖 `xai_kms`，本地未迁移。

两个 `recsys.proto` 本地冻结在 `24c6094`：`ActionName` 204–252 号段是 20260903 写死的永久分歧；新增的 `exactK`/`exactGap`、`ADS_MMP_CLICK` 本地无消费方。visibility-filtering 12 个文件本地无对应目录。

## 需要注意的一处记录修订

上一轮清点把 home-mixer 那 6 个文件判 U3 的依据之一写成「`experiment_overrides` 不在任何开源 proto 里」，本轮作废——`902a06f` 已把 `map<string, string> experiment_overrides = 21` 补进 `PredictNextActionsRequest`。成因是 `9b0dc31` 快照自身不一致：home-mixer 已在填这个字段，proto 还没跟上。

结论不变，依据换成另一半：本地没有 `util/phoenix_request.rs`（该字段唯一填充点）与 `util/strato_context.rs`，且按维护策略第 6 条不为尚未落地的能力添加参数。详见清点文档第 4 节，下轮评估以那里为准。

## 本地补丁登记表

本轮上游改动的 25 个文件与登记表的 7 个位置无交集（逐条核对），登记表原样结转，内容不变。

## 验证

- 根目录 `cargo fmt --all -- --check`：通过
- 根目录 `cargo test --workspace`：258 通过
- `cd phoenix && cargo fmt --all -- --check`：通过
- `cd phoenix && PYO3_PYTHON="$PWD/.venv/bin/python3" cargo test --workspace`：127 通过、3 ignored
- `cd phoenix && .venv/bin/python3 -m pytest tests/ -q`：100 通过
- `git diff --check`：通过

根 workspace 单测较上一锚点 +3（255 → 258），来自本地提交 `805e0fb` 的 thunder 修复，与本轮同步无关——本轮只动了两个 Python 文件。Phoenix 两侧数字均与上一锚点持平。

本轮没有新增单测：`retrieval_dataset.py` 是路径构造的等价重写，等价性用一次性脚本核对 9 条路径后确认；`log_util.py` 本地无调用方，只做冒烟验证。两项都是 U0 逐字节对齐，不属于需要回归测试的 U2 扩展。
