# 提交 d0cef2f 迁移结果（2026-08-20 上游快照）

- **提交哈希**：`d0cef2f943084ee0d4310378031c9c2c37d67f12`
- **上游父提交**：`aad7179773944e17eb8798bbbf0231d6cd6c1ffc`
- **本地策略**：按 U0-U3 语义迁移，不整体 cherry-pick
- **能力清点**：[`../upstream-sync/d0cef2f-capability-inventory.md`](../upstream-sync/d0cef2f-capability-inventory.md)
- **结果**：emb_table 可测试性重构落地；AI 趋势反馈与 served slate context 线路记录为 U3

## 1. 已落地线路

### 1.1 emb_table 可测试性拆分

提交：`phoenix: adopt d0cef2f emb_table testability split`（本记录同批）

- `load_tensor` 拆分为纯 Rust `load_tensor_into`（&str/slice 入参、`Result<_, String>`）与薄 PyO3 包装；文件与上游终态逐字节一致。

## 2. 维持 U3

- **T2 AI 趋势反馈上下文**：选择算法（top-2 趋势、频次排序、随机抽帖）已记录；阻塞点为未公开的 Strato 趋势数据合同（`batch_get_tweet_ai_trend`/`batch_get_ai_trend_name`）。
- **T3 served slate context 回路**：依附 A5 SlateContext 线路；引擎侧请求/响应合同已随 aad7179 就位，消费侧语义已记录。
- **T4 URT post_marshaller 接线**：本地无 `util/urt/` 模块。
- **T5 grox 采样与小改**：P6-B 线路。

## 3. 验证与环境事实更新

- 本轮发现并记录：phoenix Rust workspace 的 pyo3 crate 测试二进制此前在 macOS 无法执行（dyld `@rpath/Python3.framework` 加载期失败），用 `PYO3_PYTHON=phoenix/.venv/bin/python3` 重链 uv 管理的 Python 3.12 后可正常运行。已写入 `AGENTS_local.md`。
- `PYO3_PYTHON=... cargo test --workspace`（phoenix）：**116 通过、0 失败**（覆盖本次 emb_table 与此前 aad7179 落地的 SID/存储改动）。20260819 §3 的"测试二进制无法执行"结论已更正。
- 根 workspace `cargo test -p home-mixer` 与 `./scripts/run_demo.sh` 不回归。

## 4. 锚点

`upstream-first-maintenance.md` 锚点前移至 `d0cef2f`。至此 main 领先的 4 个提交（b089ce6 → 11a71f8 → aad7179 → d0cef2f）全部消化完毕，本地与 main 头对齐；后续按 upstream-first 规则等待新的上游快照。
