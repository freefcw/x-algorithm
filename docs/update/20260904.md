# 2026-09-04 上游同步记录

## 范围

清点上游 `2a38187` 与 `9b0dc31`，范围为既有同步锚点 `85ac72a..9b0dc31`。共 74 个文件，+1956/-1926。按目录：visibility-filtering 40、phoenix 15、grox 10、home-mixer 6、abuse-enforcement-service 3。有本地落点的只有 phoenix 那 15 个。

## 已吸收

- Phoenix 服务端 SID 查询客户端整体下线：历史 SID 一律从请求里读，删掉 `sid_client.rs`、两个预测服务的 `sid_client` 参数和 `--sid_endpoint` 开关。保留 `xai-recsys-sid-proto` crate 与 `reference/` 下两个 SID 服务，上游也没删。
- 顺带修上游自己遗留的两处文档漂移：`QUICKSTART.md` 的检索启动命令还在传已不存在的 `--sid_endpoint`，照着跑会直接挂；`reference/README.md` 还写着引擎用 `PySemanticIdClient` 对接这两个服务。
- `use_pinned_d2h` 默认从 `False` 翻为 `True`。约 50 GB/s 对 JAX 原生约 3 GB/s，每次推理省约 29ms，`--use_pinned_d2h false` 仍可退回。
- checkpoint 下载改为每个 trainer 开多条 HTTP/2 连接、chunk 在连接间轮转。一条连接只有一个流控窗口，之前调大 `download_concurrency` 打不破这个上限。本地补一个 `assign_trainer_channels` 单测，这个文件原本一个测试都没有。

详见 [`../upstream-sync/9b0dc31-capability-inventory.md`](../upstream-sync/9b0dc31-capability-inventory.md)。

## 未吸收

FA4 block-sparse 注意力重写与随附的 `SAVE_H100_RECSYS` remat 策略不吸收：本地 `xrex/configs/` 没有任何配置选中 `cutedsl_ranker_attn`，kernel 又硬断言 `GpuArch.GB200/GB300`；策略里的 `cutedsl_attn_outputs` 这个名字在上游整个开源树中无人产出。抄过来是无配置可达、无硬件可验的死代码。

`xai-recsys/src/util.rs` 的广告转化资产映射（约 150 行）上游无任何调用方，保持 U3。与上一轮吸收的 `fetch_mm_embeddings_into` 的区别是：那个在重构本地已有代码，这个是引入本地没有消费方的新能力。

home-mixer 6 个文件依赖闭源 proto 与本地不存在的两个 util 模块，保持 U3。visibility-filtering、grox、abuse-enforcement-service 在本地无对应模块。

社区 PR `2a38187`（VF 查询前给 `in_network_ids` 去重）不适用：本地这两个列表只装 `candidate.tweet_id`，转推与引用推走独立的 `ancillary_ids` 通道且已用 `HashSet` 去重，重复的来源不存在。

## 本地补丁登记表

上一轮暴露过一次：`ranker_attention_fa4.py` 里本地加的两行 `checkpoint_name` 藏在上游函数体内部，等上游重写整个文件才被动发现；同样的补丁在 `ranker_attention_varlen_fa4.py` 里还有一份，因为上游这次没动那个文件，第一遍没看到。

嵌在上游函数体内部的本地改动，只在上游恰好改到同一文件时才会被 diff 提醒。已在清点文档第 4 节建表登记（两处 `checkpoint_name`、`emb_table.rs` 的调度打乱移除、四处 `create_recsys_server` 包装、`--hotswap_malloc_trim`），每轮同步先对照该表，不依赖 diff 主动报警。

## 验证

- 根目录 `cargo fmt --all -- --check`：通过
- 根目录 `cargo test --workspace`：255 通过
- `cd phoenix && cargo fmt --all -- --check`：通过
- `cd phoenix && PYO3_PYTHON="$PWD/.venv/bin/python3" cargo test --workspace`：127 通过、3 ignored
- `cd phoenix && .venv/bin/python3 -m pytest tests/ -q`：100 通过
- `git diff --check`：通过

Phoenix 单测较上一锚点 −1，是随 `sid_client.rs` 一起删掉的 `semantic_ids_are_shifted_and_missing_values_remain_padding`。它覆盖的 0-indexed → 1-indexed 换算契约没有丢覆盖：现在服役的请求侧路径由 `util.rs` 的 `proto_history_semantic_ids_are_read_from_tweet_info` 守着，断言的是同一组 `[0, 7, -1] → [1, 8, 0]`。

新增的 `checkpoint_proxy` 单测在 macOS 上不计入——该模块是 `#[cfg(target_os = "linux")]`。本轮临时解开 `lib.rs` 的 cfg 门做过完整 `cargo check` 与单测（76 通过），随后还原并复验 `lib.rs` 无 diff。
