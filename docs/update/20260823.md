# 提交 28e414f 迁移结果（2026-08-21 上游快照）

- **提交哈希**：`28e414f535e4b5a50ca12ee87674e7649e50c7ad`
- **上游父提交**：`d0cef2f943084ee0d4310378031c9c2c37d67f12`
- **本地策略**：按 U0-U3 语义迁移，不整体 cherry-pick
- **能力清点**：[`../upstream-sync/28e414f-capability-inventory.md`](../upstream-sync/28e414f-capability-inventory.md)
- **结果**：引擎指标修复、proto 合同扩展与训练侧（默认关闭的）view-through/delayed-feedback 能力落地；grox 与 visibility-filtering 无落点（N/A）；xai-o2 crate 记为 U3

## 1. 已落地线路

### 1.1 引擎拒绝计数去重（E1）

`request_metrics.rs`：deadline shed 经 `guard.record_reject("deadline_admission")` 计数后，`finish` 不再按响应码重复计入 `inflight_cap`/`checkpoint_loading`；新增回归测试覆盖双计场景。文件与上游终态逐字节一致。

### 1.2 proto 合同扩展（E2）

`NextActionDistribution.slateContext = 9`；ActionName 新增 171–190（ADS MACT 点击类 6 个、view 类 6 个、view-through 转化 8 个）。Rust 与 Python 两份 `recsys.proto` 同步；Python pb2 由 hatch 构建钩子重生成，不入库。该合同是 T3 SlateContext 回路与广告特征训练的前置。

### 1.3 xrex 训练侧能力（X1–X5）

- constants 新增 MACT/view-through 映射、`ads_slim` feed、`MACT_IN_APP_LOSS_ACTION_INDICES`（12 项）与 `VIEW_THROUGH_ACTION_INDICES`（16 项）；数据侧贯穿 `conversion_keep_mask`/`sample_source` 字段。
- 模型开关 `train_view_through_heads`、`split_head_training_by_source`、`mact_in_app_loss_weight`、`metric_mask_keys` 全部默认关闭——主体逻辑与上游一致，不需要的能力保持禁用。
- trainer 候选塔分块前向降低 embedding 全量驻留内存；checkpoint 发现逻辑加固防误加载未提交 Orbax 目录；hotswap 增加 `malloc_trim` 开关。
- 配置：两塔启用 `qk_norm`/`right_anchored_rope`；`home_direct_packed_nano` 改用 `_GB300_OVERRIDES` 学习率与优化器（nano 训练配方随上游变化）。

手工合并两处本地改动：`feature_config.py` 保留 `OPTIONAL_BOOL_FEATURE_NAMES`（本地 U2）并叠加上游可选列；`README.md` 保留"本地仓库说明"块并采用上游优化器披露段。

## 2. 维持 U3 / N/A

- **O1 xai-o2 crate**：本地无 O2 存储消费方，缓议；重入条件见清单 §2。
- **G1/V1**：grox ptos/reply_spam 与 visibility-filtering 在本地已剥离，无落点。

## 3. 验证

- `PYO3_PYTHON=.venv/bin/python3 cargo test --workspace`（phoenix）：118 通过、0 失败（含新增的双计回归测试）。
- `uv run pytest`（phoenix）：92 通过、0 失败。
- 新常量经再生成的 pb2 解析验证：MACT_IN_APP_LOSS 12 项、VIEW_THROUGH 16 项、`ads_slim` 注册成功。

## 4. 锚点

`upstream-first-maintenance.md` 锚点前移至 `28e414f`。至此 origin/main 头对齐完毕；后续按 upstream-first 规则等待新的上游快照。
