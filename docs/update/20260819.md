# 提交 aad7179 迁移结果（2026-08-19 上游快照）

- **提交哈希**：`aad7179773944e17eb8798bbbf0231d6cd6c1ffc`
- **上游父提交**：`11a71f87d6a7fc4c1e8159dad8f3c5ff90a0f7ed`
- **本地策略**：按 U0-U3 语义迁移，不整体 cherry-pick
- **能力清点**：[`../upstream-sync/aad7179-capability-inventory.md`](../upstream-sync/aad7179-capability-inventory.md)
- **结果**：引擎合同与 SID 链路、xrex 训练推理更新、过滤器命名对齐均已落地；xai-o2 存储后端、grox ptos、visibility-filtering 与会话间隔线路维持 U3
- **翻修说明**：本分支未推送期间做过一次历史清理（lock 文件折入引擎合同提交、改名提交合并为单个完整改名），本文件的提交哈希为重写后的值；重写前后文件树逐字节一致

## 1. 已落地线路

### 1.1 引擎合同与 history SID 链路

提交：

- `2f11dcd phoenix: adopt aad7179 engine contract and history SID wiring`（含 phoenix/Cargo.lock；锁文件与 dev-deps 同属本批，历史清理时折入）

- 引擎 proto：`RewardOutputs`（values + policyLogits）、`ActionName` 重编号（`CLIENT_TWEET_RELEVANT_TO_SEARCH`/`NOT_RELEVANT` 161/162 → 105/106，原值改名 DEPRECATED）、`P_OPEN_LINK_P10/P25/P50/P75/P90` 分位头替换占位符、`ScoreInfo.rewardRerankSlotProb`、`SlateContext.sid*`。Python xai-proto 副本与 `hatch_build` 的 `XAI_PROTO_SKIP_MISSING` 开关同步采用。
- SID 链路补全：`stamp_semantic_ids`/`record_sid_coverage` 在 xai-recsys util 公开；history semantic IDs 从 proto 与 Arrow 列存双路径真实写入输入缓冲（此前恒零填充）；`fill_semantic_ids` 移至 `sid_client` 并带单测（+1 移位、缺失保持 padding）。
- 三方合并保留本地适配：`SAFETY_BIT_AUTHOR_NSFW` 集中化（`b7d0c6c`）与 stale-post 构造期校验（`b89df33`）。
- 存储可移植修正：O2 endpoint 缺失显式报错（原假默认值 `o2.example.invalid`）、multipart part 16→32MB。
- `MM_EMBEDDING_TTL_SECS` 环境变量覆盖按上游终态**恢复**（上游 b089ce6 移除、aad7179 恢复的 flip-flop），取代 `2ffff90` 的移除；20260817 §1.2 条目的结论以此为准。

### 1.2 xrex 训练/推理更新

提交：`5c01477 phoenix: adopt aad7179 xrex training and inference updates`

- 40+ 个与上游父提交逐字节一致的文件整体采用：Muon 优化器（新增，`optax.contrib` Newton-Schulz 正交化 + qkv split-fused + consistent RMS）、`dense_optim` 配置、rowwise_adagrad 更新、DPA 商品键双哈希与嵌入表注入、指标分位桶、model_runner 热切换重写、async_emb CUDA 与 cutedsl FA4 更新、数据加载器（kafkaloader +142 等）、五个 xrecsys 配置。
- **BoolFeature 跟随上游公开排序**：`isStalePost14d=0, isAuthorFollowedByViewerSeq=1, isAuthorFollowingViewerSeq=2`，TTL 终值 1_213_200。c65aa17 时期本地钉住的顺序（20260814 §1.3）由此取代；Rust 侧常量由 build.rs 从 `feature_config.py` 生成，自动跟随。所有消费点按枚举名取索引，重编号无行为影响。
- 本地 U2 保留：`OPTIONAL_BOOL_FEATURE_NAMES` 继续把三个 bool 列排除在 `REQUIRED_COLUMNS` 之外，旧数据集缺列零填充语义不变。
- 构造点不变量保持：model_runner / sid_retrieval_runner 合并上游重写的同时保留 `create_recsys_server` 工厂为唯一构造入口（AST 测试：4 个工厂调用、0 个直接构造）。
- 顺序敏感修正：三处 `IS_STALE_POST14D + 1` 宽度推导（util 测试 ×2、stale 测试 ×1）与引擎 stale 校验改为三常量最大值 + 1，不再假设枚举顺序。

### 1.3 home-mixer 过滤器命名对齐

提交：`43bf036 home-mixer: rename muted keyword filter to viewer-scoped`

- `MutedKeywordFilter` → `ViewerMutedKeywordFilter`（文件、类型、装配点、文档引用）。本地实现语义已等价（正文+引用文本匹配），纯命名对齐，无行为变化。

## 2. 明确不迁移 / 维持 U3

- **P3 xai-o2 存储后端**（`XaiO2Store`/`get_store`/`XAI_RECSYS_S3_BACKEND`）：依赖未公开 crate `xai_o2`（上游发布的 Cargo.toml 亦无声明）。重入条件：crate 公开或 U1 适配器替代。
- **H2/H4/H5 会话间隔线路**：`FollowingViewerMutedKeywordFilter`、`ancestor_texts` 字段、`conversation_gap_ancestor_hydrator`、`quoted_tweet_text` Option 化。本地无数据源，重入时含失败回落语义。
- **H3 QuotedPostTextHydrator**：本地已由 `CoreDataCandidateHydrator` 等价覆盖，不重复引入。
- **grox ptos 七项**（prior_nsfw 先验等）：P6-B 线路。
- **visibility-filtering 五项**（dark_traffic_setup 等）：既定不采用决策覆盖。

## 3. 验证

- `cargo test -p xai-recsys-proto`：21 通过；`cargo test -p xai-recsys`：23 通过（含 3 个新 SID 测试与 2 个重编号修正后的 relation bool 测试）。
- `cargo check -p xai-recsys-engine --tests`、`cargo check -p xai-recsys-mm-server` 通过。（补充：后经 `PYO3_PYTHON` 重链本机 venv Python 3.12，引擎测试二进制已可执行，见 [`20260820.md`](20260820.md) 与 `AGENTS_local.md`；此前记录的 macOS dyld 限制已解除。）
- `cd phoenix && uv run pytest`：92 通过（含更新后的 schema 测试）；`compileall xrex/` 干净。
- 根 workspace `cargo test -p home-mixer`：6 个测试二进制全部通过（改名回归）。
- `./scripts/run_demo.sh`：端到端链路验证通过。

## 4. 锚点

`upstream-first-maintenance.md` 锚点前移至 `aad7179`。下一清点对象：`d0cef2f`（2026-08-20，AI 趋势反馈上下文 hydrator + `emb_table.rs` 大重构）。
