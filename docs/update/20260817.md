# 提交 b089ce6 迁移结果（2026-08-17 上游快照）

- **提交哈希**：`b089ce64891f9c50fab73aa00dbe65acb82f198f`
- **上游父提交**：`c65aa179db7bdd61e2c2821eac87f208a105c053`（前一吸收锚点）
- **本地策略**：按 U0-U3 语义迁移，不整体 cherry-pick
- **能力清点**：[`../upstream-sync/b089ce6-capability-inventory.md`](../upstream-sync/b089ce6-capability-inventory.md)
- **结果**：可移植主线已落地；默认路径行为不变；完整 Demo 不回归

## 1. 已落地线路

### 1.1 作者 NSFW 安全位生产侧接线

提交：`5725f97 home-mixer: wire author NSFW safety mask producer side`

上游 b089ce6 在候选→引擎请求映射中写入 `safetyLabelMask`（原创帖且作者命中 Phoenix NSFW 口径时置 `SAFETY_BIT_AUTHOR_NSFW`）。本地以 U0 合同补齐方式落地：

- `PostCandidate` 新增 `nsfw_author_phoenix: Option<bool>`，缺省 `None`。
- 网关合同 `proto/definitions/recsys.proto` 的 `TweetInfo` 新增 `safety_label_mask`（字段 4），位语义对齐 Phoenix 引擎合同的 `TweetInfo.safetyLabelMask`/`Candidate.safetyLabelMask`。
- `SAFETY_BIT_AUTHOR_NSFW` 由 `x-algorithm-proto` 根部导出，与 phoenix 侧 `xai-recsys-proto` 同名常量同值（phoenix 侧测试锁定 `1 << 2 == 4`）。
- `phoenix_scorer` 置位规则：原创帖且 `Some(true)` 才置位，转帖不置位；新增单测覆盖原创命中、转帖命中、数据源缺失、明确未命中四种情况。
- 数据源（Gizmoduck 作者安全标签推导）为 U3，重入条件：作者安全标签进入 TES core-data 或等价合同。当前所有候选该字段为 `None`，mask 恒 0，行为不变。
- demo 网关的 `RecsysBatch` 无 safety 槽位，demo 模型不消费该字段；消费侧为 xrex 生产特征路径（`candidate_seq["safety_label_mask"]`）与 Rust 引擎（`util.rs:530`）。
- **后续（2026-08-22 复验确认）**：生产侧接线已整体回退——`PostCandidate` 移除 `nsfw_author_phoenix`，`phoenix_scorer` 恒发 `safety_label_mask = 0`，本条新增的单测一并删除，**本条目不再代表现状**。proto 合同字段（`TweetInfo.safety_label_mask`）与 phoenix 侧消费逻辑（`safety_filter.py`、Rust 引擎 `util.rs`）保留；重入条件不变（作者安全标签数据源落地后按 U0 合同重新接线）。

### 1.2 MM embedding 缓存 TTL 去环境变量化

提交：`2ffff90 phoenix: drop env override for mm embedding TTL`

- 移除 `embedding_ttl()` 与 `MM_EMBEDDING_TTL_SECS`，统一回常量 `EMBEDDING_TTL`（2 天）。
- 采用上游移除的理由：writer/reader 进程共享 `/dev/shm` 缓存分片，TTL 必须一致，env 覆盖会破坏该不变量；本地无文档化的覆盖需求。
- 改后文件与上游 b089ce6 版本逐字节一致。
- **后续**：上游在 aad7179 恢复了该 env 覆盖（flip-flop），本地已按终态跟随恢复，见 [`20260819.md`](20260819.md) §1.1。本条目不再代表现状。

## 2. 明确不迁移 / 重分类

### 2.1 followers 仅原创帖设置（上游 A4 修正）

落地时重分类为 U3：上游修的是引擎请求映射中"转帖携带转发作者粉丝数"的串扰；本地网关 `TweetInfo` 没有 followers/authorInfo 字段、不存在任何 followers 转发链路，该 bug 在本地没有载体。重入条件：未来引入 followers 转发时直接采用原创帖限定语义。

### 2.2 品牌安全 verdict V2（A1/A2）

`compute_verdict_v2`、V2 标签集与 `EnableAdsBrandSafetyVerdictV2` 开关依赖未公开的 `xai_x_thrift` V2 标签变体与本地不存在的 ads 管线，U3 记录（语义与重入条件见清点文档 §3）。

### 2.3 SlateContext SID 多样性统计（A5）

算法可移植但依附本地未引入的 `SlateContext` 与候选 `semantic_ids` 两条链路，U3 记录。注意点：该统计在请求内即时计算即可供给 slate 特征，不强制依赖请求缓存持久化。

### 2.4 其余 U3 与无模块项

A6（following 尺寸常量）、A7（客户端事件 Kafka 会话计数口径）、A8（URT pill facepile 门控）、B2（Strato rankall 数据流口径）、C1–C4（grox ptos 交叉验证/Kafka 限流/GrokShare/reply_spam 拆分，走 P6-B 重入条件）、D1（bdsm 执法门槛 sentinel 化）、D2（abuse-enforcement Kafka 手动提交）——语义均已记录在清点文档，本地无对应模块或链路，不落地代码。

## 3. 验证

- `cargo test --workspace`：16 个测试二进制全部通过（含新增 `author_nsfw_bit_only_set_for_original_posts`）。
- `cargo fmt --all --check` 与 `cargo clippy -p x-algorithm-proto -p home-mixer --all-targets` 无告警。
- `cargo check -p xai-recsys-mm-server`（phoenix workspace）通过。
- `cd phoenix && uv run pytest`：92 passed。
- `./scripts/run_demo.sh`：端到端链路验证通过（35 条 Feed，网内 4 + 网外 31）。

## 4. 锚点

`upstream-first-maintenance.md` 的 absorbed anchor / upstream head 已前移至 `b089ce6`。下一清点对象：`11a71f8`（2026-08-18），其主体是 visibility-filtering 接入，与既定的"不采用 visibility filtering"决策（见 `20260813.md` P1 记录）直接相关，清点时需逐项区分 VF 部分与可独立吸收的 brand_safety/cold_start 修正。
