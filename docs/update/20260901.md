# 2026-09-01 上游同步记录

## 范围

清点上游 `bc8e5f0` 与 `6384ca7`，范围为既有同步锚点 `24c60942..6384ca7`。两个提交共 79 个文件，+4064/-1777。

## 已吸收

- Phoenix copy-port transfer join、Bytes gRPC parser、embedding table 分块拷贝。
- Phoenix 多模态 embedding 类型配置与 MM cache 同步/并行接口。
- xrex `driver.py` / `driver_local.py` 重命名为 `core.py` / `local.py`，并更新引用。

详见 [`../upstream-sync/6384ca7-capability-inventory.md`](../upstream-sync/6384ca7-capability-inventory.md)。

## 未吸收

`reranker_head_tag`、Page Decode、广告 delayed conversion、visibility-filtering safety label/golden corpus、加密 checkpoint、Kafka mTLS、Grox special video、abuse enforcement、SimClusters offline 以及 Brazil 名单更新均未直接进入当前分支；原因是缺少本地可执行合同、模型消费者或部署依赖。

## 验证

- 根目录 `cargo fmt --all -- --check`：通过
- 根目录 `cargo test --workspace`：255 通过
- `cd phoenix && cargo fmt --all -- --check`：通过
- `cd phoenix && PYO3_PYTHON="$PWD/.venv/bin/python3" cargo test --workspace`：125 通过、3 ignored
- `git diff --check`：通过

系统默认 `python` 不存在，系统 `python3` 版本不满足项目使用的 `match` 语法，因此未以系统 Python 运行测试。
