# 02 — 优化方案与实现

> 针对 [01-背景与瓶颈分析](./01-背景与瓶颈分析.md) 中识别出的 A–F 六个点做的具体改造。
> OOM / 数据量（点 G）在下一篇单独说。

---

## 1. 方案选型

上一篇列出 7 个方向，实施时按"风险 × 收益"分三档：

| 档位 | 内容 | 风险 | 收益估算 |
|---|---|---|---|
| 第 1 档 | A（重写 Parquet 加载）+ B（去 per-step 同步）+ C-模拟数据向量化 | 低 | 高 |
| 第 2 档 | + D（嵌入表上设备 + `jnp.take` 融合到 `train_step`）+ E（简单异步预取） | 中 | 中 |
| 第 3 档 | + bfloat16 混合精度 | 中高 | 依赖硬件 |

本次落地 **第 1 + 2 档**（用户确认）。第 3 档暂不启用，原因是本机主要跑 CPU/Metal 无 GPU，混合精度收益未必正向；且 CPU 上 bf16 反而可能更慢。

---

## 2. 改造清单

### 2.1 【A】Parquet 加载：一次读入 + flatten/reshape

**旧逻辑的两个问题**：
1. `pq.read_table → to_pydict → np.array(list[list[int]])` 是 Python 逐元素路径；
2. 每个文件只取 `batch_size` 行，然后外层 while 每轮重开所有文件。

**新逻辑**：用 pyarrow 的 ListArray `.values.to_numpy()` 直接拿到扁平 buffer，再 `reshape` 回来。由于本项目每条样本内 list 长度是**固定的**（`HISTORY_LEN=32`、`NUM_CANDIDATES=8`、`NUM_HASHES=2`、`NUM_ACTIONS=19`），reshape 成立。

核心函数：

```@/Users/hejun/work/mp/x-algorithm/phoenix/scripts/train_ranker.py:262-272
def _col_to_numpy(col, tail_shape: tuple, dtype):
    """把一个 pyarrow list 列（可能嵌套两层）一次性展开并 reshape 为紧凑 numpy 数组。

    假设每条样本中 list 的长度都相同（本项目固定为 HISTORY_LEN / NUM_CANDIDATES / NUM_HASHES /
    NUM_ACTIONS），这样可以直接取底层 buffer 并 reshape，避免 Python 逐元素转换。
    """
    a = col.combine_chunks()
    for _ in tail_shape:
        a = a.values
    flat = a.to_numpy(zero_copy_only=False)
    return flat.reshape(-1, *tail_shape).astype(dtype, copy=False)
```

和外面的列规格表（一次写定，两种模式共用）：

```@/Users/hejun/work/mp/x-algorithm/phoenix/scripts/train_ranker.py:275-286
# 每列的 numpy shape（不含 batch 维）与 dtype，供流式加载复用。
_PARQUET_COLUMN_SPEC = {
    "user_hashes": ((NUM_HASHES,), np.int32),
    "history_post_hashes": ((HISTORY_LEN, NUM_HASHES), np.int32),
    "history_author_hashes": ((HISTORY_LEN, NUM_HASHES), np.int32),
    "history_actions": ((HISTORY_LEN, NUM_ACTIONS), np.float32),
    "history_product_surface": ((HISTORY_LEN,), np.int32),
    "candidate_post_hashes": ((NUM_CANDIDATES, NUM_HASHES), np.int32),
    "candidate_author_hashes": ((NUM_CANDIDATES, NUM_HASHES), np.int32),
    "candidate_product_surface": ((NUM_CANDIDATES,), np.int32),
    "labels": ((NUM_CANDIDATES, NUM_ACTIONS), np.float32),
}
```

> 注：为什么用 `astype(..., copy=False)`？pyarrow 里列的实际底层 dtype 可能是 int32 也可能是 int64（历史遗留老文件）。`copy=False` 让同 dtype 时做 no-op，跨 dtype 时才强制拷贝，用一份代码兼容两种来源。

**内存 Dataset + 无限切片迭代器**：

```@/Users/hejun/work/mp/x-algorithm/phoenix/scripts/train_ranker.py:380-397
def iterate_parquet_dataset(
    data: dict,
    batch_size: int,
    shuffle: bool = True,
    seed: int = 0,
):
    """把内存中的数据按 batch_size 无限循环切片产出 (RecsysBatch, labels)。

    每个 epoch 结束会重新 shuffle 索引。
    """
    n = len(data["user_hashes"])
    if n < batch_size:
        raise ValueError(f"样本数 {n} 小于 batch_size {batch_size}")
    rng = np.random.default_rng(seed)
    while True:
        idx = rng.permutation(n) if shuffle else np.arange(n)
        for start in range(0, n - batch_size + 1, batch_size):
            yield _make_batch_labels(data, idx[start:start + batch_size])
```

这个迭代器无限循环，**彻底消除外层 while 每轮重开 Parquet 目录的浪费**（对应瓶颈 E）。

---

### 2.2 【B】去掉 per-step `float(loss_val)` 同步

旧代码把 loss 当作 Python float 累加，强制每步 block_until_ready：

```python
loss_accum += float(loss_val)  # 旧
```

新代码把 loss 保留在 device 上累加，只在 `log_every` 步做一次同步：

```@/Users/hejun/work/mp/x-algorithm/phoenix/scripts/train_ranker.py:601-616
    loss_accum = jnp.zeros((), dtype=jnp.float32)
    accum_count = 0
    step = 0

    while step < args.steps:
        batch, labels = next(data_iter)
        # tree_map 下异步 device_put，下一步 train_step 会与之流水重叠
        batch_dev = jax.tree_util.tree_map(jnp.asarray, batch)
        labels_dev = jnp.asarray(labels)

        params, opt_state, loss_val = train_step(params, opt_state, batch_dev, labels_dev)
        loss_accum = loss_accum + loss_val
        accum_count += 1
        step += 1

        if step % args.log_every == 0:
            avg_loss = float(loss_accum) / max(accum_count, 1)
```

这个一行改动的意义远大于它看上去的简单：**JAX 的 `@jax.jit` 是异步分派**，CPU 不再 per-step 等待 device 完成，下一步的 batch 准备、H2D 拷贝、train_step 调度都可以和上一步的 device 计算重叠流水线。

---

### 2.3 【C】向量化模拟数据

原 `runners.create_example_batch` 里有两个 Python for，每个 step 都跑。改写成一份主训练循环专用的向量化版：

```@/Users/hejun/work/mp/x-algorithm/phoenix/scripts/train_ranker.py:205-259
def make_simulated_batch_fast(
    batch_size: int, rng: np.random.Generator
) -> tuple[RecsysBatch, np.ndarray]:
    """向量化版模拟 batch，去除 create_example_batch 里的 Python for 循环。

    主训练循环每步都调用；实测单步构造时间比原版下降一个数量级以上。
    """
    B = batch_size
    user_hashes = rng.integers(1, TABLE_SIZE, size=(B, NUM_HASHES), dtype=np.int32)
    ...
    # 向量化随机截断历史（valid_len 在 [HISTORY_LEN//2, HISTORY_LEN] 内均匀）
    valid_len_post = rng.integers(HISTORY_LEN // 2, HISTORY_LEN + 1, size=B)
    valid_len_author = rng.integers(HISTORY_LEN // 2, HISTORY_LEN + 1, size=B)
    pos = np.arange(HISTORY_LEN)[None, :]
    history_post_hashes = np.where(
        (pos < valid_len_post[:, None])[:, :, None], history_post_hashes, np.int32(0)
    )
    ...
```

`runners.create_example_batch` 仍然保留，供 `make_simulated_batch`（init/dummy 阶段）和其它模块使用。

---

### 2.4 【D】嵌入表上设备 + 查表融合进 `train_step`

关键设计：**嵌入表不参与梯度更新**（它们是离线 npz 预计算的），所以可以作为**闭包常量**放进 JIT 过的 `train_step` 里，XLA 会直接把它们当成 HLO 参数编译进去。

```@/Users/hejun/work/mp/x-algorithm/phoenix/scripts/train_ranker.py:540-567
    # 2. 嵌入表上设备（一次性，之后 jnp.take 全部在设备上完成）
    user_emb_dev = jax.device_put(jnp.asarray(user_emb))
    post_emb_dev = jax.device_put(jnp.asarray(post_emb))
    author_emb_dev = jax.device_put(jnp.asarray(author_emb))
    ...

    # 5. JIT 编译 train_step：查表与前反向一起融合，避免 per-step host↔device 拷贝。
    def jax_lookup(batch: RecsysBatch) -> RecsysEmbeddings:
        return RecsysEmbeddings(
            user_embeddings=user_emb_dev[batch.user_hashes],
            history_post_embeddings=post_emb_dev[batch.history_post_hashes],
            candidate_post_embeddings=post_emb_dev[batch.candidate_post_hashes],
            history_author_embeddings=author_emb_dev[batch.history_author_hashes],
            candidate_author_embeddings=author_emb_dev[batch.candidate_author_hashes],
        )

    @jax.jit
    def train_step(params, opt_state, batch, labels):
        def loss(p):
            embeddings = jax_lookup(batch)
            return loss_transform.apply(p, batch, embeddings, labels)
        loss_val, grads = jax.value_and_grad(loss)(params)
        updates, new_opt_state = optimizer.update(grads, opt_state)
        new_params = optax.apply_updates(params, updates)
        return new_params, new_opt_state, loss_val
```

收益：
- host 侧不再做 numpy fancy indexing；
- host↔device 拷贝从"每步 3 张嵌入切片"降到"每步仅 batch 里的整型索引"；
- 查表和 `train_step` 的前反向被 XLA 合并优化。

> ⚠️ 如果将来嵌入表需要参与训练，这个写法就要改：嵌入表必须作为 `params` 的一部分，而不是闭包常量。

---

### 2.5 【E】异步预取（轻量版）

这里没有引入 `threading.Queue` 之类的后台线程，而是靠 JAX 的**异步默认行为**：

```python
batch_dev = jax.tree_util.tree_map(jnp.asarray, batch)   # 发起异步 H2D
labels_dev = jnp.asarray(labels)                          # 同上
params, opt_state, loss_val = train_step(...)             # 立刻异步分派
# 只要下一步不在 .block_until_ready / float(loss)，下一步的数据准备就会和
# 这一步的 device 计算重叠执行。
```

**前提**：数据源本身在 CPU 上是"便宜"的（现在内存切片 or 流式读 parquet 都能满足）。如果数据侧本身有 IO 抖动，才需要考虑真正的后台线程预取。

---

## 3. 落地前后代码骨架对比

```
             旧                                      新
─────────────────────────────────────   ─────────────────────────────────────
while step < steps:                      load_parquet_dataset(...)  # 一次性
  open all parquet files                 data_iter = iterate_parquet_dataset(...)
  for file in files:
    read WHOLE file → to_pydict          while step < steps:
    take first batch_size rows             batch = next(data_iter)
    numpy(nested list)                     batch_dev = tree_map(jnp.asarray, batch)
    lookup_embeddings(cpu numpy)           params, opt, loss_val = train_step(
    jnp.array(labels)                          params, opt, batch_dev, labels_dev)
    train_step(...)                        loss_accum = loss_accum + loss_val  (device)
    loss_accum += float(loss_val)  ← sync  if step % log_every == 0: float(loss_accum)  (sync)
```

---

## 4. 不动的东西

- `loss_fn` 本身、`PhoenixModelConfig`、`TransformerConfig`、`runners.create_example_batch`、`recsys_model.PhoenixModel` 都没改。
- `pyproject.toml` / `uv.lock` 不变。
- Checkpoint 格式不变（`flatten_dict` + `np.savez`）。
- Embedding table 保存路径不变（`embedding_tables.npz`）。

改动范围**完全限定**在 `scripts/train_ranker.py`。

---

## 5. 验证

见 [04-基准测试与效果.md](./04-基准测试与效果.md)。

下一篇 → [03-OOM与流式训练.md](./03-OOM与流式训练.md)
